# 方案设计：产品表面缺陷视觉检测系统

---

## 01 立项依据

### 1.1 问题定义

制造产线的表面质量检测目前主要依赖人工目检，存在三个核心矛盾：产线速度越来越快与人眼检测速度有限的矛盾、产品精度要求越来越高与人眼一致性差的矛盾、质量追溯要求越来越高与人工检测数据难以结构化的矛盾。

本项目旨在用机器视觉替代人工目检，构建一套可运行的缺陷检测系统，验证 AI 编程方法在工业视觉软件开发中的效率与可行性。

### 1.2 目标量化

| 指标 | 目标值 | 衡量方式 |
|------|--------|----------|
| 缺陷分类准确率 | ≥ 95% | NEU 测试集 Top-1 Accuracy |
| 目标检测 mAP@0.5 | ≥ 0.80 | GC10-DET 验证集 |
| 单张推理延迟 | ≤ 500ms（CPU） | YOLOv8n 640×640 输入 |
| 系统可运行性 | 一键启动 | 执行启动脚本后界面可访问 |
| 检测界面 | 5 个功能页面 | Streamlit 多页面可切换 |
| AI 编程占比 | ≥ 70% 代码由 AI 辅助生成 | Git 提交记录与开发日志 |

### 1.3 约束条件

- **时间约束**：11 周完成从环境搭建到答辩的全流程
- **数据约束**：仅使用公开数据集，不依赖企业内部数据
- **硬件约束**：开发机无独立显卡时需保证 CPU 可推理（YOLOv8n）
- **技术约束**：全栈使用 Python，不引入 JavaScript 前端框架
- **成本约束**：AI 编程工具月费控制在 200 元以内

---

## 02 系统能力清单

### 2.1 用户故事

以用户故事格式描述系统功能，每条包含角色、需求、价值与验收标准。

**US-01 单张图像检测**
作为质检操作员，我希望上传一张产品表面图片后系统自动检测并标注缺陷，以便快速判断该产品是否合格。
验收标准：支持 JPG/PNG/BMP 上传；检测结果在原图上以矩形框标注缺陷位置与类别；显示合格/不合格判定。

**US-02 批量图像检测**
作为班组长，我希望导入一个文件夹后系统逐张检测并输出汇总表，以便统计整批产品的合格率。
验收标准：支持文件夹批量导入；输出包含文件名、缺陷数、判定结果的汇总表；可导出 CSV。

**US-03 检测方法切换**
作为算法工程师，我希望在传统方法、图像分类、目标检测三种方法间切换对比，以便评估不同方案的优劣。
验收标准：界面提供模型选择下拉框；切换后重新检测；显示各方法的推理耗时。

**US-04 预处理过程可视化**
作为学习者，我希望看到图像从原始到分割的每一步处理结果，以便理解算法原理。
验收标准：并排展示灰度化、滤波、阈值、边缘检测四步结果；参数可调节并实时刷新。

**US-05 质量趋势统计**
作为质量主管，我希望查看按批次的合格率趋势和缺陷类型分布，以便发现质量问题规律。
验收标准：提供合格率折线图和缺陷分布饼图；支持按批次筛选。

**US-06 检测历史回溯**
作为审计人员，我希望按时间或批次查询历史检测记录，以便追溯质量问题。
验收标准：支持时间范围和批次号检索；可查看单条记录的原图与检测结果。

**US-07 检测报告导出**
作为质检员，我希望将检测结果导出为报告，以便归档和提交。
验收标准：单张检测可导出带标注图片；批次检测可导出汇总报告。

### 2.2 优先级划分

- **Must Have（必须有）**：US-01、US-02、US-03
- **Should Have（应该有）**：US-04、US-05
- **Could Have（可以有）**：US-06、US-07
- **Won't Have（本期不做）**：摄像头实时检测、模型在线训练、多用户权限管理

---

## 03 关键技术决策

采用 ADR（Architecture Decision Record）格式记录每项关键技术决策。

### ADR-01：目标检测框架选择

- **背景**：需要一个目标检测模型实现缺陷定位与分类
- **选项**：YOLOv8、Faster R-CNN、SSD、DETR
- **决策**：采用 YOLOv8n（nano 版本）
- **理由**：Ultralytics 提供极简 Python API，三行代码完成训练与推理；nano 版本仅 6MB，CPU 可实时运行；预训练权重丰富，迁移学习效果好；AI 生成相关代码准确率极高
- **后果**：检测精度可能不如大模型，但满足课程设计需求；如需更高精度可切换 YOLOv8s

### ADR-02：分类模型选择

- **背景**：需要一个图像分类模型作为缺陷类型判定的对比方案
- **选项**：ResNet18、MobileNetV3、EfficientNet、VGG16
- **决策**：ResNet18 为主，MobileNetV3 为轻量级对比
- **理由**：ResNet18 是迁移学习经典基线，torchvision 内置预训练权重，文档丰富；MobileNetV3 用于对比模型大小与精度的权衡
- **后果**：ResNet18 模型约 44MB，推理速度适中；MobileNetV3 约 10MB，适合边缘部署讨论

### ADR-03：传统检测方法选择

- **背景**：需要一个不依赖深度学习的基线方法，用于对比和可解释性分析
- **选项**：阈值分割加 SVM、模板匹配、HOG 加 SVM、局部二值模式加分类器
- **决策**：Otsu 阈值分割加轮廓提取，提取几何与 GLCM 纹理特征，用 SVM 分类
- **理由**：流程清晰可解释，每一步结果可视化，适合教学展示；Scikit-learn 提供成熟 SVM 实现
- **后果**：对复杂背景和光照变化敏感，精度上限有限，但作为基线足够

### ADR-04：前端框架选择

- **背景**：需要一个 Web 界面展示检测结果
- **选项**：Streamlit、Gradio、Flask 加 HTML、Vue.js
- **决策**：Streamlit
- **理由**：纯 Python 开发，无需前后端分离；内置图像组件和 Plotly 图表；多页面支持；开发效率比 Vue.js 高一个数量级
- **后果**：界面定制能力有限，但满足课程设计需求

### ADR-05：后端框架选择

- **背景**：需要提供检测推理 API
- **选项**：FastAPI、Flask、Django
- **决策**：FastAPI
- **理由**：原生异步、自动生成 Swagger 文档、Pydantic 数据校验、性能高
- **后果**：需要学习 Pydantic 模型定义，但学习成本低

### ADR-06：数据集选择

- **背景**：需要公开数据集支撑分类与检测任务
- **选项**：NEU、MVTec AD、KolektorSDD、GC10-DET、DAGM2007
- **决策**：NEU（分类主力）+ GC10-DET（检测主力）+ MVTec AD（异常检测扩展）
- **理由**：NEU 6 类缺陷每类 300 张，适合分类；GC10-DET 已标注 bbox，省去自行标注；MVTec AD 是异常检测标准基准
- **后果**：NEU 原始数据无 bbox 标注，检测任务依赖 GC10-DET

### ADR-07：AI 编程工具组合

- **背景**：需要选择 Vibe Coding 工具链
- **选项**：Cursor 单独、Aider 单独、Cursor 加 Aider、Claude Code
- **决策**：Cursor（主力编码）+ Aider（Git 提交与重构）
- **理由**：Cursor 的 Composer 多文件编辑和 Inline Edit 适合日常开发；Aider 的自动 commit 和 Repo Map 适合版本管理与重构
- **后果**：需要维护两套工具的 API Key，但成本可控

---

## 04 数据与模型方案

### 4.1 数据集明细

**NEU 表面缺陷数据集**

- 提供方：东北大学
- 对象：热轧钢表面
- 类别：裂纹、夹杂、斑块、麻点、氧化皮、划痕（共 6 类）
- 规模：每类 300 张，共 1800 张，200×200 灰度图
- 划分：训练 70%（1260 张）、验证 15%（270 张）、测试 15%（270 张），分层抽样
- 用途：传统方法与分类模型的训练评估

**GC10-DET 钢板缺陷数据集**

- 提供方：阿里云天池
- 类别：10 类钢板缺陷
- 标注：已提供 YOLO 格式 bounding box
- 用途：YOLOv8 目标检测训练评估

**MVTec AD 数据集**

- 提供方：MVTec Software GmbH
- 规模：15 类工业产品，5354 张高分辨率图
- 标注：缺陷区域像素级分割掩码
- 用途：PaDiM 无监督异常检测验证（扩展功能）

### 4.2 模型方案

**模型一：SVM 传统分类器（基线）**

- 输入：手工特征向量（几何特征 + GLCM 纹理特征 + 灰度特征，约 20 维）
- 算法：Scikit-learn SVC，RBF 核，C=10
- 预处理：StandardScaler 标准化
- 预期准确率：≥ 85%

**模型二：随机森林（传统对比）**

- 输入：同 SVM
- 算法：RandomForestClassifier，n_estimators=200，max_depth=15
- 附加输出：feature_importances_ 用于可解释性分析
- 预期准确率：≥ 88%

**模型三：ResNet18 迁移学习（深度学习分类）**

- 输入：224×224 RGB（灰度图复制三通道）
- 结构：ImageNet 预训练 ResNet18，替换 fc 为 6 类输出
- 训练策略：冻结前 5 层，微调后 3 层 + fc；Adam lr=1e-4；余弦退火；Label Smoothing
- 数据增强：Albumentations（随机裁剪、旋转、翻转、亮度对比度扰动、高斯噪声）
- 预期准确率：≥ 95%

**模型四：YOLOv8n（目标检测，主力）**

- 输入：640×640 RGB
- 结构：YOLOv8n 原生，COCO 预训练迁移
- 训练：100 epochs，batch=16，SGD lr=0.01，早停 patience=20
- 推理：conf=0.25，iou=0.45 NMS
- 输出：bbox(x1,y1,x2,y2) + class + confidence
- 预期 mAP@0.5：≥ 0.80

**模型五：PaDiM（无监督异常检测，扩展）**

- 输入：224×224 RGB
- 原理：ResNet18 多尺度 patch 特征拟合多元高斯分布，马氏距离作为异常分数
- 优势：无需缺陷样本，训练仅需前向传播一次
- 预期 AUC：≥ 0.90

### 4.3 评估方法

| 模型类型 | 评估指标 |
|----------|----------|
| 分类模型 | Top-1 Accuracy、Precision、Recall、F1、混淆矩阵 |
| 目标检测 | mAP@0.5、mAP@0.5:0.95、Precision、Recall、FPS |
| 异常检测 | AUC、Pixel-level AUROC、推理速度 |
| 全系统 | 端到端延迟、批量处理吞吐量、界面响应时间 |

---

## 05 工程实现

### 5.1 技术栈总览

| 层级 | 技术 | 版本 |
|------|------|------|
| 语言 | Python | ≥ 3.10 |
| 图像处理 | OpenCV、Pillow、scikit-image、NumPy | OpenCV ≥ 4.8 |
| 数据增强 | Albumentations | ≥ 1.4 |
| 传统 ML | Scikit-learn | ≥ 1.4 |
| 深度学习 | PyTorch、torchvision、Ultralytics | PyTorch ≥ 2.2 |
| 后端 | FastAPI、Uvicorn、Pydantic | FastAPI ≥ 0.110 |
| 数据库 | SQLite + SQLAlchemy | SQLAlchemy ≥ 2.0 |
| 前端 | Streamlit、Plotly | Streamlit ≥ 1.30 |
| 日志 | Loguru | ≥ 0.7 |
| 质量 | Ruff、Mypy、Pytest | - |
| AI 编程 | Cursor、Aider | - |

### 5.2 模块划分

```
surface-defect-inspection/
├── app.py                  # Streamlit 入口
├── main.py                 # FastAPI 入口
├── requirements.txt
├── config/
│   └── settings.yaml       # 模型路径、阈值、类别配置
├── data/
│   ├── neu/                # NEU 数据集
│   ├── gc10/               # GC10-DET 数据集
│   └── mvtec/              # MVTec AD 数据集
├── src/
│   ├── preprocessing/      # 图像预处理与特征提取
│   ├── models/             # 五种模型的统一接口实现
│   ├── api/                # FastAPI 路由与数据模型
│   ├── pages/              # Streamlit 多页面
│   └── utils/              # 绘图、标注、日志工具
├── tests/                  # 单元测试
├── notebooks/              # 实验笔记
└── docs/                   # 项目文档
```

### 5.3 核心接口定义

**检测推理接口**

- 请求：POST /api/detect，multipart/form-data，包含 image 文件、model_name 字符串、conf_threshold 浮点数
- 响应：JSON，包含 image_id、detections 数组（每项含 class_name、confidence、bbox）、inference_time_ms、is_defective 布尔值

**批量检测接口**

- 请求：POST /api/batch-detect，上传 zip 文件或文件夹路径
- 响应：JSON，包含 total_count、defective_count、pass_rate、results 数组

**模型管理接口**

- GET /api/models：列出可用模型及状态
- POST /api/models/switch：切换当前默认模型

### 5.4 前端页面规划

| 页面 | 核心组件 | 数据来源 |
|------|----------|----------|
| 检测工作台 | 文件上传器、模型选择器、阈值滑块、结果图像、判定卡片 | /api/detect |
| 批量检测 | 文件夹上传、进度条、汇总数据表、导出按钮 | /api/batch-detect |
| 预处理可视化 | 原图、灰度、滤波、阈值、边缘五图并排、参数滑块 | 本地 OpenCV 处理 |
| 质量统计 | 批次选择器、合格率折线图、缺陷分布饼图、指标卡 | SQLite 历史记录 |
| 检测历史 | 时间筛选、批次筛选、记录列表、详情弹窗 | SQLite 历史记录 |

### 5.5 图像处理管线

原始图像依次经过以下步骤，每步结果可在预处理可视化页面查看：

1. 灰度化（cvtColor）
2. 高斯滤波去噪（GaussianBlur，核大小可配）
3. CLAHE 自适应直方图均衡化（对比度增强）
4. Otsu 自适应阈值分割（二值化）
5. 形态学开运算（去除小噪点）
6. 形态学闭运算（填充缺陷内部孔洞）
7. Canny 边缘检测
8. 轮廓查找与筛选（按面积、周长、圆形度过滤）

---

## 06 执行时间线

### 6.1 里程碑

| 里程碑 | 截止周 | 交付标准 |
|--------|--------|----------|
| M1 环境就绪 | 第1周末 | Python 环境、Git、Cursor/Aider 配置完成，数据集下载并可加载 |
| M2 传统方法可用 | 第3周末 | 预处理模块 + SVM/RF 分类器，准确率 ≥ 85%，有评估报告 |
| M3 深度学习分类可用 | 第4周末 | ResNet18 分类模型，准确率 ≥ 95%，与传统方法对比完成 |
| M4 目标检测可用 | 第5周末 | YOLOv8n 模型，mAP@0.5 ≥ 0.80，推理可视化正常 |
| M5 后端 API 可用 | 第7周末 | FastAPI 全部接口通过 Swagger 测试 |
| M6 前端界面可用 | 第8周末 | Streamlit 5 个页面可交互，前后端联调通过 |
| M7 系统集成完成 | 第9周末 | 全流程可运行，单元测试覆盖率 ≥ 70%，无阻塞 Bug |
| M8 答辩就绪 | 第11周末 | 文档齐全、演示视频录制完成、答辩 PPT 准备完毕 |

### 6.2 逐周任务

- 第1周：环境搭建、数据集下载探索、数据加载器、选题说明
- 第2周：图像预处理模块（灰度、滤波、阈值、形态学、边缘）
- 第3周：手工特征提取、SVM/RF 训练评估、传统方法报告
- 第4周：Albumentations 增强管线、ResNet18/MobileNetV3 训练对比
- 第5周：YOLOv8 数据准备、训练、推理可视化、调优
- 第6周：FastAPI 骨架、检测接口、批量接口、模型管理接口
- 第7周：记录存储、报告生成、接口单元测试
- 第8周：Streamlit 5 页面开发、前后端联调
- 第9周：性能优化、边界处理、Bug 修复、测试补充
- 第10周：README、API 文档、使用手册、代码注释
- 第11周：演示视频、答辩 PPT、模拟答辩、仓库整理

### 6.3 风险登记

| 风险 | 概率 | 影响 | 缓解措施 |
|------|------|------|----------|
| 传统方法特征区分度不足，准确率低 | 中 | 中 | 增加 GLCM 等纹理特征维度，尝试多种分类器组合 |
| 分类模型过拟合 | 中 | 高 | 强化 Albumentations 数据增强，加入 Dropout 和权重衰减 |
| YOLOv8 标注数据不足 | 低 | 高 | 使用已标注的 GC10-DET，必要时用 LabelImg 补充标注 |
| 前后端图像传输超时 | 低 | 中 | base64 编码传输，限制上传图片大小，异步处理批量任务 |
| AI 工具 API 额度耗尽 | 中 | 低 | 备用 DeepSeek 低成本 API，国产工具（通义灵码）兜底 |
| 时间不足 | 中 | 高 | 优先完成 Must Have 用户故事，Could Have 功能可裁剪 |

---

## 07 参考资料

**缺陷检测领域**

[1] Luo Q, et al. Automated visual defect detection for flat steel surface: A survey. IEEE Transactions on Instrumentation and Measurement, 2020.

[2] Tabernik D, et al. Segmentation-based deep-learning approach for surface-defect detection. Journal of Intelligent Manufacturing, 2020.

[3] Song K, Yan Y. A noise robust method based on completed local binary patterns for hot-rolled steel strip surface defects. Applied Surface Science, 2013.

**深度学习与目标检测**

[4] He K, et al. Deep residual learning for image recognition. CVPR, 2016.

[5] Redmon J, et al. You only look once: Unified real-time object detection. CVPR, 2016.

[6] Howard A, et al. Searching for MobileNetV3. ICCV, 2019.

[7] Jocher G, et al. Ultralytics YOLOv8. https://github.com/ultralytics/ultralytics, 2023.

**传统图像处理**

[8] Otsu N. A threshold selection method from gray-level histograms. IEEE SMC, 1979.

[9] Canny J. A computational approach to edge detection. IEEE PAMI, 1986.

[10] Haralick R M, et al. Textural features for image classification. IEEE SMC, 1973.

[11] Cortes C, Vapnik V. Support-vector networks. Machine Learning, 1995.

**异常检测**

[12] Defard T, et al. PaDiM: A patch distribution modeling framework for anomaly detection. ICPR, 2021.

[13] Bergmann P, et al. MVTec AD: A comprehensive real-world dataset for unsupervised anomaly detection. CVPR, 2019.

**AI 编程**

[14] Vaithilingam P, et al. Grounded copilot: How programmers interact with code-generating models. CHI, 2023.

**工具文档**

[15] FastAPI Documentation. https://fastapi.tiangolo.com, 2024.

[16] Streamlit Documentation. https://docs.streamlit.io, 2024.

[17] Ultralytics YOLOv8 Documentation. https://docs.ultralytics.com, 2024.

[18] OpenCV Documentation. https://docs.opencv.org, 2024.

[19] Albumentations Documentation. https://albumentations.ai, 2024.

[20] Aider: AI pair programming in your terminal. https://aider.chat, 2024.
